/** @type {import('tailwindcss').Config} */
export default {
  darkMode: "media", // ← auto: follows user's OS theme
  content: ["./index.html", "./src/**/*.{js,jsx}"],
  theme: { extend: {} },
  plugins: [],
};
