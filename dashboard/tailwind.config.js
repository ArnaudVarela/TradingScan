/** @type {import('tailwindcss').Config} */
export default {
  darkMode: "class", // ← auto: follows user's OS theme
  content: ["./index.html", "./src/**/*.{js,jsx}"],
  theme: { extend: {} },
  plugins: [],
};
